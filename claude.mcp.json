{
  "mcpServers": {
    "session-bridge": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/cli.js",
        "mcp",
        "--host",
        "claude"
      ]
    }
  }
}
